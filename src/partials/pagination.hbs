<nav class="pagination-nav" role="navigation">
    {{#if prev}}
        <a class="previous" href="{{page_url prev}}">&larr; Newer Posts</a>
    {{/if}}

    <span>Page {{page}} of {{pages}}</span>

    {{#if next}}
        <a class="next" href="{{page_url next}}">Older Posts &rarr;</a>
    {{/if}}
</nav>
